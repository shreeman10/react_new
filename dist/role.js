function redirect(role) {
    if (role === "admin") {
        console.log("this is admin");
        return;
    }
    if (role === "user") {
        console.log("this is user");
        return;
    }
    role;
}
function hotel1(order) {
    console.log(order);
}
function hotel2(order) {
    console.log(order);
}
const cup = {
    name: "newCup",
    type: "order",
    size: "small"
};
const config = {
    id: 15,
    cores: 4,
    threads: 8,
};
config.cores = 6;
console.log(config);
export {};
//# sourceMappingURL=role.js.map